import React from 'react';

interface StagingGridProps {
  extractedData: any[];
  onClear: () => void;
  onConfirm: () => void;
}

export const StagingGrid: React.FC<StagingGridProps> = () => {
  return null;
};
